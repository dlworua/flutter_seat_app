import 'package:flutter/material.dart';

//SeatBottom StatelessWidget 생성
class SeatBottom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('SeatBottom');
  }
}
